{
  "name": "@sihsalus/fhir-client",
  "version": "0.1.0",
  "private": true,
  "license": "MPL-2.0",
  "description": "Typed FHIR R4 client wrapper for SIH Salus",
  "main": "src/index.ts",
  "source": true,
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typescript": "tsc",
    "lint": "eslint src --ext ts,tsx",
    "test": "vitest run --passWithNoTests"
  },
  "peerDependencies": {
    "@openmrs/esm-framework": "workspace:*"
  }
}
